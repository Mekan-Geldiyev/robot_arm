# Tested working versions - mediapipe in particular changes its API across
# versions (this project uses the Tasks API, not the older mp.solutions.pose),
# so these are pinned rather than left open-ended.
opencv-python==5.0.0.93
mediapipe==0.10.35
pyserial==3.5
